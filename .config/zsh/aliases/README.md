# Aliases

Crea archivos dentro del directorio aliases/user.d con tus propios alias que seran automaticamente cargados a tu entorno. Estos archivos seran ignorados por yadm para no generar conflictos.

Los aliases en estos archivos sobreescribiran los alias propuestos en aliases/base.zsh